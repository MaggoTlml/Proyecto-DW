module.exports = {
    secret: "bezkoder-secret-key"
  };
  
  //funciones jsonwebtoken como o utilizar un algoritmo que necesita una clave 
  //secreta (como String) para codificar y decodificar el token.verify()sign()
  //Puede crear su propia cadena.secret
  